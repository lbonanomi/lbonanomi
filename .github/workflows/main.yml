name: Labeler

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    env:
      TOKEN: ${{ secrets.TOKEN }}   

    steps:
    - name: User Nutrition Label
      uses: lbonanomi/nutrition-label@1.0
      with:
        count-forked: 'false'
