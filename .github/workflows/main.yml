name: Labeler

on:
  schedule:
    - cron: "30 0/12 * * *"

jobs:
  generate_content_label:
    runs-on: ubuntu-latest

    env:
      TOKEN: ${{ secrets.TOKEN }}   

    steps:
    - name: User Nutrition Label
      uses: lbonanomi/nutrition-label@1.3
      with:
        count-forked: 'false'
